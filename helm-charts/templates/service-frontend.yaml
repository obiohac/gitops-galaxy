apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    {{- include "sherlock-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.frontendPort }}
    targetPort: 80
    name: http
  selector:
    app: frontend